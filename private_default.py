cookies = {}
cookies["session"] = "" # à compléter et ne pas partager !

# Où trouver le cookie ?
# Menu inspecter (ctrl+maj+i) -> Application -> Cookies -> https://adventofcode.com

# Attention de ne pas partager !
# Pour effacer l'historique du fichier private (si besoin)
# git filter-branch --force --index-filter "git rm --cached --ignore-unmatch private.py" --prune-empty --tag-name-filter cat -- --all
# git push --force --verbose --dry-run
# git push --force
